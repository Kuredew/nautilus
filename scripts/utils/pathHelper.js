import { join } from "path";

export const sourcePath = "src";
export const libFolderName = "lib";
export const outputFolderName = "dist";

export const libFolderPath = join(sourcePath, libFolderName);
